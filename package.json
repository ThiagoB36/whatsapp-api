{
  "name": "whatsapp-api",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run db:generate && npm run db:deploy",
    "start": "npm run start:prod",
    "start:prod": "node dist/src/main.js",
    "db:generate": "prisma generate",
    "db:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@evolution-api/api": "^2.1.1"
  },
  "devDependencies": {
    "@prisma/client": "^5.0.0",
    "prisma": "^5.0.0"
  }
}
